{
  "version": 2,
  "build": {
    "env": {
      "NODE_ENV": "production"
    }
  },
  "buildCommand": "npm install -g typescript && npm install && tsc",
  "outputDirectory": ".",
  "routes": [
    {
      "src": "/",
      "dest": "bot.js"
    }
  ]
}